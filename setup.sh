mkfs.fat -F32 /dev/nvme0n1p5 && mkfs.ext4 /dev/nvme0n1p6 && mount /dev/nvme0n1p6 /mnt && mkdir -p /mnt/boot && mount /dev/nvme0n1p5 /mnt/boot && pacstrap /mnt base linux-zen linux-firmware base-devel nano && genfstab -U /mnt >> /mnt/etc/fstab && arch-chroot /mnt /bin/bash -c "ln -sf /usr/share/zoneinfo/Asia/Bangkok /etc/localtime && hwclock --systohc && sed -i 's/#en_US.UTF-8 UTF-8/en_US.UTF-8 UTF-8/' /etc/locale.gen && locale-gen && echo 'LANG=en_US.UTF-8' > /etc/locale.conf && echo 'archlinux' > /etc/hostname && echo 'root:toor' | chpasswd && pacman -S --noconfirm grub networkmanager efibootmgr os-prober ntfs-3g && echo 'GRUB_DISABLE_OS_PROBER=false' >> /etc/default/grub && grub-install --target=x86_64-efi --efi-directory=/boot --bootloader-id=GRUB --recheck && grub-mkconfig -o /boot/grub/grub.cfg && systemctl enable NetworkManager" && umount -R /mnt && reboot
