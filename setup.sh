mkfs.fat -F32 /dev/nvme0n1p4 && mkfs.ext4 /dev/nvme0n1p5 && mount /dev/nvme0n1p5 /mnt && mkdir /mnt/boot && mount /dev/nvme0n1p4 /mnt/boot && pacstrap /mnt base linux linux-firmware && genfstab -U /mnt >> /mnt/etc/fstab && arch-chroot /mnt /bin/bash -c "ln -sf /usr/share/zoneinfo/Asia/Bangkok /etc/localtime && hwclock --systohc && echo 'en_US.UTF-8 UTF-8' >> /etc/locale.gen && locale-gen && echo 'LANG=en_US.UTF-8' > /etc/locale.conf && echo 'bedrock-linux' > /etc/hostname && echo 'root:toor' | chpasswd && pacman -S --noconfirm grub networkmanager efibootmgr && grub-install --target=x86_64-efi --efi-directory=/boot --bootloader-id=GRUB && grub-mkconfig -o /boot/grub/grub.cfg" && umount -R /mnt && reboot